# the-orb

Personal reflection pipeline. Walks → transcripts → tone analysis → insights → iMessage.

Private. Local. Accumulating.

## Layout
- `src/` — pipeline code
- `config/` — launchd plist, philosophy lens prompts
- `data/` — SQLite, transcripts (gitignored)
- `logs/` — pipeline logs (gitignored)
